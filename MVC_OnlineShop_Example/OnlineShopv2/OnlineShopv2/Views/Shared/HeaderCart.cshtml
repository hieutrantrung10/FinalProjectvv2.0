@model List<OnlineShopv2.Models.CartItem>
<div class="cart">
    <p>
        Chào mừng các bạn đến với website HomeShop! <span>Giỏ hàng:</span><div id="dd" class="wrapper-dropdown-2">
            @Model.Count sản phẩm@{ decimal total = 0;} @foreach (var item in Model)
                                                          {
                                                              total += (item.Product.Price.GetValueOrDefault(0)*item.Quantity);
                                                          }
        @total.ToString("N0") đ
            <ul class="dropdown">
                <li>Hãy kiểm tra giỏ hàng của bạn</li>
            </ul>
        </div>
    </p>
</div>
